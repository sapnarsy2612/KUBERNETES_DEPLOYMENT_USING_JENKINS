kind: Service                             
apiVersion: v1
metadata:
  name: sapna
  labels:
    app: sapna
spec:
  ports:
    - port: 8080                               
      targetPort: 80                    
      nodePort: 31200
  selector:
    app: sapna                  
  type: LoadBalancer
